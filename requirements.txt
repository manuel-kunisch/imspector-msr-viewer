numpy>=1.26
tifffile>=2023.4.12
PyQt5>=5.15   # only needed for the viewer
imageio-ffmpeg   # only for video export (or ffmpeg on PATH)
pyqtgraph        # only for the PSF analysis and the line scan
scipy            # optional: Gaussian fit, spline sampling, image shifts
scikit-image     # optional: sub-pixel alignment in the line scan (numpy fallback otherwise)
matplotlib       # optional: line scan plot as PNG / PDF / SVG
